import React from 'react';
import {Container} from '../component/Container'
const Search = (props) => {
return (
    <Container>
        To be finished!
    </Container>
  )
};

export default Search;